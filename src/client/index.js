import {
  getTravelData,
  updateTripInfo,
  handleFormSubmit,
  handleTripButtons,
} from "./js/app.js";
import "./styles/style.scss"; // Import the SCSS file

handleFormSubmit();
handleTripButtons();
